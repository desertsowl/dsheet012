<div class="container">
    <h1 class="mb-4"><%= title %></h1>
    <p><%= message %></p>
    <div class="mt-3 d-flex gap-2">
        <a href="<%= backLink %>" class="btn btn-secondary">戻る</a>
        <% if (typeof addLink !== 'undefined') { %>
            <a href="<%= addLink %>" class="btn btn-primary">読込</a>
        <% } %>
    </div>
</div>
